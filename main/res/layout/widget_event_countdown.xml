<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="16dp">

    <!-- Header row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="10dp">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_widget_logo"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="EventManager"
            android:textColor="#9494AA"
            android:textSize="11sp"/>

        <TextView
            android:id="@+id/widget_countdown_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/widget_countdown_badge"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:text="J-3"
            android:textColor="#FFFFFF"
            android:textSize="11sp"/>
    </LinearLayout>

    <!-- Event title -->
    <TextView
        android:id="@+id/widget_event_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Marrakech Music Festival 2026"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginBottom="6dp"/>

    <!-- Date -->
    <TextView
        android:id="@+id/widget_event_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="15 Juin 2026 • 20h00"
        android:textColor="#9494AA"
        android:textSize="12sp"
        android:layout_marginBottom="2dp"/>

    <!-- Location -->
    <TextView
        android:id="@+id/widget_event_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Palmeraie, Marrakech"
        android:textColor="#9494AA"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end"/>

    <!-- Spacer -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <!-- Footer hint -->
    <TextView
        android:id="@+id/widget_empty_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Touchez pour voir vos billets"
        android:textColor="#E63946"
        android:textSize="11sp"
        android:visibility="gone"/>

</LinearLayout>